/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "source.unsplash.com",
      "picsum.photos",
      "scontent.cdninstagram.com",
    ],
  },
};

export default nextConfig;
